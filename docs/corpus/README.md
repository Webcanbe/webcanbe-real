# Independent corpus provenance

Both directories under `fixtures/independent` are unchanged, MIT-licensed official Vite starter templates. They are **INDEPENDENT PROJECT** evidence at the starter level, not complete external applications or evidence of broad product compatibility. Every file is preserved, including template underscore filenames. No locks or configuration changes were added.

Each JSON receipt in this directory records the upstream repository, exact commit, directory, per-file Git blob SHA-1 and SHA-256, and empty modifications list. The corresponding LICENSE files retain the complete upstream MIT license. Tests verify these bytes before compile/import; separate real controlled rendering and sandboxed export/build/render acceptance are recorded in the [Phase 2G.1 report](../reports/phase2g1-hosted-foundation-hmr-profiles.md).

The existing fixtures and `fixtures/profile-kitchen` are **AUTHORED FIXTURE** evidence. Disposable QA instrumentation and exports are not committed. Substantial independently authored full-project acceptance remains open for Phase 2G.2.
