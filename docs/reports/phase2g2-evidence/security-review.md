# Phase 2G.2 separate security review

Completed 2026-09-14. **No active reportable finding remains in the reviewed surfaces after remediation. Coverage is PARTIAL. This is not Phase 2 closure, hosted readiness, or an independent professional penetration test.**

The installed Codex Security standard prompt-only desktop workflow was actually used. Scan `aa887637-bd30-4b3e-a8f1-335f0d050502` completed successfully through semantic draft validation and one finalization call. The tool generated `report.md`, `scan-manifest.json`, `findings.json`, `coverage.json` and SARIF in its authoritative private scan directory. Canonical coverage retains the six original candidates, evidence and remediation decisions. No named third-party scanner or professional audit is claimed. Aggregate token usage was unavailable.

The tool reported: **“Working-tree contents changed while the scan was running; results were saved for the original snapshot.”** Its target identity therefore remains the launch snapshot at `cac7b3abe33a26dcb4f5110b67a8433840960b95`, not a certificate for the final committed tree. Implementation remediation continued during review. The reviewer reread the final relevant code after the implementation freeze and independently checked every non-Markdown hash in [implementation-digests.json](implementation-digests.json), with zero mismatches. This distinction is preserved in the canonical report.

A separate reviewer coordinated a fresh context baseline auditor, a fresh architecture mapper and a fresh focused identity/storage/fencing investigator. The baseline fully reviewed 57 files; the focused investigator reviewed 24, with overlap. Their results plus final source re-review cover **62 distinct fully reviewed files**, against 355 paths in the discovery inventory. Architecture mapping and search hits are not counted as completed audits. Remaining paths are explicitly deferred in canonical coverage. Review workers read current source offline and did not execute application code. One native Node `ServerResponse` check confirmed duplicate response headers throw; the implementation agent executed the application and isolation tests below. Preflight was ready with a four-slot capacity warning. Daybreak access was `not_granted`; that advisory did not create or imply protected-tool coverage.

| Discovered issue | Remediation and verification | Qualification |
| --- | --- | --- |
| Revoked PostgreSQL user could receive a fresh registered session | Durable disabled-user rows; shared identity lock for register/revoke/issue; fresh grant lookup rejects disabled users. Real PostgreSQL regression covers revoke followed by new issue/register denial. | Source review confirmed the adapter path; no deployed account compromise was reproduced. |
| Artifact put queued behind project deletion could retain a new payload | Exact undeleted project/workspace row lock, required row result and authorization after the lock. Regression observes a real PostgreSQL lock wait, commits delete, then checks put rejection and zero active objects. | The original get path already denied deleted projects; this was deletion/retention integrity, not proven cross-tenant disclosure. |
| Rejected source saves could grow durable history without a quota | Ledger limited to 64 MiB, 1000 transactions and 1001 revisions; journal limited to 160 MiB; existing file size checked before parsing. Accepted/rejected writes check capacity before canonical writes. PostgreSQL applies the same history bound. | Tests preserve exact source/history at capacity. Automatic compaction is absent; safe capacity refusal requires operator archival. |
| PostgreSQL URL TLS options could override checked pool TLS policy | Production pool rejects `ssl*` URL query parameters before enforcing verified CA configuration. Regressions cover disabled verification and CA override options. | Conditional deployment configuration weakness; no browser-selected database URL or actual MITM was demonstrated. Local-test TLS exceptions remain explicit. |
| Async CSRF failure occurred after HTTP 200 headers | Await CSRF rotation before writing headers; error path avoids a second header write. Local HTTPS regression injects async failure and checks clean 403 and unchanged session lifetime. | Native response behavior was independently checked; no service outage was reproduced. |
| Identity mapping disable could race with fresh session issuance | PostgreSQL callback selects atomic `issueVerifiedIdentity`; active mapping, disabled-user check and session insertion share the transaction/identity lock used by disable and revoke. Fresh focused re-review confirmed ordering. | Sequential disable/issue regression passed; shared-lock source analysis establishes concurrency ordering. Mapping disable blocks future login; `revokeUser` invalidates existing sessions. Generic optional fallback adapters are not made atomic by this fix. |

The implementation agent supplied passing execution receipts; the reviewer read their contents and relevant test source but did not independently rerun them. [validation-summary.json](validation-summary.json) records `npm test`: 282 passed with 23 explicitly gated skips; the real PostgreSQL/mTLS-runner/React18+19 command then passed 24 tests, including those 23 and one shared compiler test. That is **305 distinct passing tests, including all 263 unchanged original tests**. It also preserves the earlier Vite cleanup `ENOTEMPTY` failure and unchanged-suite passing rerun. TypeScript and build passed. [browser-regressions.json](browser-regressions.json) records five retained browser harnesses. [engineering-measurements.json](engineering-measurements.json) records six standalone sandbox export builds/renders and their isolation probes.

| Reviewed boundary | Observed controls and remaining limit |
| --- | --- |
| Auth, session, project authorization | Fixed verified-TLS OIDC endpoints, signed issuer/audience/nonce/time checks, one-use cookie-bound PKCE state, server-owned mappings, hashed short-lived sessions, exact Host/Origin, CSRF, role/membership epochs and durable user revoke. Full hosted application composition remains open. |
| Canonical source, history and artifacts | Private checkouts, source/history transaction acceptance, revision/epoch CAS, immutable history prefixes, rejected-receipt append without source revision change, exact readback epoch, tenant tuple/digest checks and retirement tombstones. PostgreSQL role is trusted server authority; schema alone is not tenant RLS or cloud durability proof. |
| Runner commands, resources and recovery | Fixed launcher/stop arguments, immutable admitted job hash, mTLS gateway, database clock/locks/fences before and after operations, per-host admission, stale-controller rejection, explicit cleanup and quarantine. Unknown/live generations cannot be reported successfully revoked. Final host behavior still needs real deployed evidence. |
| Preview, browser and mutation authority | Strict mode sends PNG/observations, keeps imported executable modules in controlled Chromium, validates source selection and current frame/generation, and uses an opaque viewer. Hosted viewer helper supplies exact frame-ancestors, CSP/sandbox and Permissions-Policy. Local Blob mode remains explicitly weaker and is not a zero-egress fallback. |
| Paths, secrets and export | Archive/path/link/size checks, private materialization and cleanup, confined compiler dependencies, disabled uploaded Babel/config execution, fixed runtime env and no project access to platform/mTLS/database credentials. Export preserves registered real source; independent build/config execution is a separate local macOS QA sandbox. |

[runner-isolation.json](runner-isolation.json) contains real local Lima namespace/cgroup/native-Chromium evidence: collector positive controls, zero isolated collector records, filesystem/process/socket probes, memory termination and a frozen-worker watchdog. It expressly does not claim a complete exploit corpus or full DTLS/SCTP exfiltration proof. [local-tls-browser.json](local-tls-browser.json) records real local Chromium on distinct `.test` sites with test certificate pinning: Secure/HttpOnly/Strict host-only cookies, denied opaque-viewer storage/parent access, separate viewer state, CSRF and cross-project source denial. These are local boundary observations, not deployed DNS, production IdP or cloud isolation proof.

The smallest software gate remains a concrete asynchronous hosted editor/API composition connecting PostgreSQL identity/session authority, fresh project authorization, canonical source/history/artifact storage and the real runner provider. The current hosted runner harness injects its owner authorization callback; this does not prove an end-to-end hosted identity-to-runner chain. Production composition must require the atomic identity issuance path. After that, real identity credentials, managed durable backend/recovery, separately deployed runner hosts and real app/viewer DNS/TLS/cookie behavior must be validated. Operational key ownership, least-privilege database grants, backup/retention and actual host enforcement cannot be inferred from configuration examples. None of these open gates is replaced by the completed source review.
