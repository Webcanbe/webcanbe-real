# Focused compatibility implementation ledger

- Baseline/remote/ancestry/main/clean state verified. Reused final P01–P64 ledger; original 429 test identities and file hashes preserved as the floor.
- Threat model recorded before source changes. Existing limits and no-egress boundary retained. Independent scoped architecture review pending.
- Static HTML/config group: 42 focused+retained passes. Alternate IDs/body/head/local stylesheet, constant nested aliases, same-directory TS extends, explicit root/publicDir and finite test/optimize settings. Unknown build/plugin semantics still refuse. Initial new Blob test incorrectly reused a fixture with root-public imports; replaced only that new unit fixture's App with authored static content. Existing tests unchanged.
- Inert metadata group found an existing secret-bearing URL-path regression when allowing public example endpoints. Restored generic credential-word path rejection; targeted verification underway. No existing assertion changed. Recognized bounded Bun binary framing is preservation-only; runtime graph remains unsupported.
- Independent baseline architecture review completed (12 resources, 98 source anchors); evidence retained in baseline-architecture.json.
- Yarn classic/Bun text: 14 focused passes, including compiled projects with complete operator-matching graphs. Berry/npm alias protocols and binary Bun runtime graphs remain refused. New profiles add finite Tailwind 3/PostCSS and UnoCSS dependencies; no old profile lock changed.
- CSS: 11 focused passes. Uploaded modules are static AST data; trusted compiler runs in disposable 128 MiB workers, 8 s deadline, at most two. Arbitrary plugin functions, filesystem content globs and Browserslist module imports refuse.
- Public values and static assets: 51 focused passes after source-origin SVG data distinction. Operator-only workspace/project provider, fresh authorization before/after async work. HTTPS public IPv4 DNS pinned per hop; 4 transfers, 10 s operation, 2 MiB/member, 8 MiB total, 48 URLs, 3 redirects; no HTTP decompression or remote active MIME. Snapshot-scoped content-addressed cache/audit; runner unchanged and egress denied.
- Application failures retained: Redux still exceeds 2 MiB member; Bulletproof and Todo intake now PASS. Initial Recipe shell refusal led to finite inert HTML metadata handling. Sequential and then partially batched fetches hit unchanged 10 s deadline; single image 241 ms, classic font response 5865 ms / about 6.5 MiB TTF. Fixed modern negotiation + four-way resource batches fetched all 38 remote resources; original SVG data-image preservation needed an explicit distinction from forbidden remote SVG. Final application rerun pending.
- Automatic approval review rejected a proposed lock parse/profile-selection fallback. That proposal was omitted; lock failure still blocks runtime. No bypass implemented.
- First broad default invocation: all 494 owned cases passed and 45 skipped, but temporary upstream corpus tests were inadvertently collected (14 missing-import collection failures). Not counted as a successful suite. Corrected QA deletes materialized source after each case and subsequent commands target src/webcanbe-engine explicitly. Original tests unchanged; preserve this harness failure.
