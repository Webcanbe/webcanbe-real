const path=require('node:path'),{spawnSync}=require('node:child_process');const root=path.resolve(__dirname,'../..');const result=spawnSync(path.join(root,'.webcanbe/runner/tools/bin/limactl'),['shell','--workdir=/','wcb','sudo','-n','/usr/bin/node','/opt/wcb-runtime/verify.cjs'],{env:{...process.env,LIMA_HOME:path.join(root,'.webcanbe/runner/lima')},stdio:'inherit',timeout:110000});if(result.error)throw result.error;process.exit(result.status??1);
