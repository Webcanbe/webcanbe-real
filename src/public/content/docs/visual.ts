import { family } from '../doc-types'
export default family('Visual editor', [
['visual-editor','Visual editor','Visual editing changes supported source definitions through the rendered interface. It preserves the project’s source rather than replacing it with a separate canvas format.','Open a working copy with a usable preview. A page rendering correctly does not mean every element or style has a safe editable source target; inspect the capabilities shown for the selected element.',[
'Switch to Visual and enable element selection. Select a visible element and check its source context.',
'Choose a supported text or style control. Inspect whether the change affects a shared definition and which authoring breakpoint is active.',
'Apply the change and wait for acceptance. Check the preview and the resulting History entry.',
'Switch to Code when a control is unavailable, a value is inherited or the source requires a more complex change.'
],'The accepted source should contain the change, with the preview reflecting that revision. Code and export should expose the same source result.','Visual editing is deliberately bounded. Dynamic expressions, unresolved styles and shared instances may require Code. A disabled control should explain a boundary rather than pretending a mutation succeeded. Do not interpret preview-only selection as permission to alter every rendered node.',['visual-editor/select-elements','visual-editor/shared-source','visual-editor/unsupported']],
['visual-editor/select-elements','Select elements','Element selection connects a rendered target to a source definition that the inspector can understand. Use the selection mode before trying to change text or styles.','Connect the preview and choose Visual or Split. Interactive controls inside the project can consume clicks in interaction mode, so distinguish selecting a button from activating that button.',[
'Enable Select elements in the workspace preview controls. Move to the visible target and select it.',
'Inspect the target details and available capabilities. Confirm that you selected the intended element rather than its container or a repeated instance.',
'Use an available text or style control for a small change. If no safe target is available, inspect the corresponding file in Code.',
'After saving, reselect the element if the source structure changed. Check the accepted revision before continuing.'
],'The inspector should show a source-backed target and only the controls supported for it. Selection itself should not mutate the source or create a history entry.','Elements generated from complex expressions, third-party rendering or unresolved source can be unavailable. A stale selection after a structural edit should be refreshed rather than forced. If selection fails across the whole preview, check the preview connection and compatibility report first.',['visual-editor/text','visual-editor/unsupported','working-copies/preview']],
['visual-editor/text','Edit visible text','Use Visual text editing when the selected element exposes a supported text capability. Use Code for text generated by logic, translations or data that the visual inspector cannot safely rewrite.','Select a source-backed element and inspect its text control. Decide whether the text is local to this element or comes from a shared component rendered in several places.',[
'Select the visible text element in selection mode. Confirm that the editable value matches the source you intend to change.',
'Enter the replacement text without unrelated markup or code. Apply the text change through the inspector.',
'Wait for acceptance and inspect the preview at relevant widths, because longer text can change wrapping and layout.',
'Check other instances of the component and review History. Use Code if the value is derived from props, an expression or an external source.'
],'The source should contain the intended text change and the rendered result should match it. Export after saving should include that accepted text.','Not every visible string is a directly editable literal. A disabled text control is a compatibility boundary, not a request to retry repeatedly. For shared source, changing the definition can affect multiple instances; undo deliberately if the scope was broader than intended.',['visual-editor/shared-source','code-editor','visual-editor/responsive']],
['visual-editor/spacing','Change spacing and layout','Spacing controls change supported source style properties such as padding, margin and gaps. Inspect the layout context before changing values, because a child’s position can be controlled by its parent.','Select the relevant element and identify the active authoring breakpoint. Check style origins so you know whether a value is locally editable, inherited or defined by a rule that requires Code.',[
'Inspect the container and child structure. Use padding for space inside a box, margin for supported outside spacing, and gap where the layout exposes it.',
'Change one value at a time through an available inspector control. Save to the intended source scope and breakpoint.',
'Check the preview at narrow and wide widths. Look for overflow, collapsed spacing and changes to neighboring elements.',
'For complex flex or grid behavior, open the source in Code and inspect the container rules together rather than guessing from one child.'
],'The accepted style should explain the observed layout change in source. Related elements should remain aligned at the widths you checked.','Visual support depends on the source pattern and property. An inherited or unresolved value may be read-only. Preview viewport selection does not automatically make a style responsive; the authoring breakpoint determines where the saved declaration applies.',['visual-editor/responsive','visual-editor/style-origins','code-editor/styles']],
['visual-editor/typography-colors','Edit typography and colors','Use supported typography and color controls to refine a project while keeping its source styles understandable. Check readability and shared design definitions before changing an isolated value.','Identify whether the element uses a shared token, class, inline style or inherited rule. A visible font may also depend on an external asset or a license that is separate from the source project.',[
'Select the element and inspect the effective styles and their origins. Choose an editable property such as a supported size, weight or color.',
'Apply a small change at the intended authoring breakpoint. Avoid replacing a shared design token merely to fix one instance without inspecting the source.',
'Check text wrapping, contrast and control states in the preview. Review headings, links and nearby body text together.',
'Use Code when the value comes from an unsupported expression, inherited rule or shared token that needs a coordinated change.'
],'The saved source should contain a deliberate style change and the result should remain readable across the checked widths. Shared components should not acquire inconsistent styling accidentally.','The inspector cannot certify accessibility or grant font rights. Keep required asset licenses and verify contrast with an appropriate tool in your own review. Unsupported or ambiguous style origins should remain read-only until you make an explicit source edit.',['visual-editor/style-origins','visual-editor/shared-source','visual-editor/responsive']],
['visual-editor/responsive','Edit responsive styles','Preview width and authoring breakpoint serve different purposes. Preview width shows a layout; the authoring breakpoint determines which supported source rule receives a style change.','Inspect the selected element’s existing responsive definitions. A base declaration can affect wider layouts unless another rule overrides it, while an override applies only under its source condition.',[
'Choose a preview viewport to inspect the problem. Then separately check the inspector’s active authoring breakpoint.',
'Review effective and inactive style values before editing. Identify whether you need to change the base rule or an existing responsive override.',
'Save through the appropriate style action, including Save at viewport when offered for the supported source pattern.',
'Recheck neighboring widths and the base layout. Open Code to inspect media queries or utility classes if the result is unexpected.'
],'The source change should land in the intended responsive scope, and the preview should agree at the widths where that scope applies. Changing viewport alone should not create a revision.','Do not assume that selecting a phone-sized preview automatically isolates all edits to mobile. Existing source rules and supported authoring capabilities control the result. Complex or unresolved responsive styles may require Code rather than another visual override.',['visual-editor/style-origins','code-editor/styles','working-copies/preview']],
['visual-editor/shared-source','Understand shared source definitions','A rendered component can appear many times while sharing one source definition. Visual edits to that definition can affect all matching instances.','Before editing a repeated card, button or navigation item, inspect the source context and effect scope. Distinguish a shared definition from instance-specific props or data.',[
'Select the element and inspect whether the workspace reports shared source. Look for other visible instances of the same component.',
'Choose a source-wide edit only when all instances should change. Review the proposed or accepted scope before continuing.',
'If one instance needs different content or styling, use Code to inspect the component API and its props rather than forcing an unsafe visual instance edit.',
'After saving, check multiple instances and responsive states. Use History to inspect or reverse a change whose scope was wrong.'
],'A shared-source edit should behave consistently wherever that definition renders. An instance-specific change should be represented explicitly in source, such as through an existing supported prop or data value.','The visual layer may refuse instance-only mutations when the source is shared. That protects source consistency; it is not a rendering failure. Do not duplicate an entire component simply to bypass the warning without considering maintainability.',['visual-editor/text','code-editor','history/undo-redo']],
['visual-editor/style-origins','Inspect style origins','Style origins explain why a selected element has its current appearance and whether a declaration can be edited safely. Effective, inactive and inherited values can come from different source locations.','Select a source-backed target and inspect its style information before changing a value that appears to be overridden. The rendered result alone cannot identify which declaration should be rewritten.',[
'Find the effective value and its source origin. Compare it with inactive declarations and the active breakpoint.',
'Edit a supported local declaration when it matches your intended scope. Save and inspect the source revision.',
'If the value is inherited, unresolved or read-only, open the relevant source in Code and inspect the parent or shared rule.',
'Recheck the preview after saving and confirm that another rule is not overriding the new value.'
],'You should be able to explain which source declaration controls the result. A successful edit should not depend on invisible preview-only overrides.','The inspector does not guarantee support for every CSS pattern, dynamic class construction or runtime style injection. Repeatedly adding stronger values can make the source harder to maintain. When origin resolution is ambiguous, prefer a deliberate Code edit and verify all affected states.',['visual-editor/spacing','visual-editor/typography-colors','code-editor/styles']],
['visual-editor/unsupported','When to use Code','Use Code when Visual cannot safely identify or update the source pattern you need. Partial visual compatibility is compatible with a useful editable project.','Read the selected target’s capability and error message. Separate a disconnected preview from a source pattern that is intentionally unsupported, because they require different fixes.',[
'Check whether selection works elsewhere in the same preview. If no targets resolve, inspect the preview connection first.',
'For a specific unsupported target, locate its component or style definition in Code. Inspect shared props, expressions and inherited rules.',
'Make a bounded source change and save it. Resolve syntax or revision errors before making further changes.',
'Return to the preview and inspect the result. Visual controls may become available if the source is now supported, but that is not required for the code edit to be valid.'
],'The change should exist in accepted source even if Visual continues to expose only a subset of controls. History and export should include the saved edit.','Do not remove meaningful application logic just to make an element visually editable. Third-party components, dynamic styling and generated content can require continued Code work. Compatibility is a report of supported operations, not a judgment that all other source is invalid.',['code-editor','compatibility/visual-support','troubleshooting/visual-target']],
])
