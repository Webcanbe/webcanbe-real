/** Verified local-preview boundary, not a claim of complete browser egress containment. */
export const PREVIEW_SECURITY_NOTICE = "Opaque preview: parent DOM and storage access are denied; CSP restricts fetch and resource loads. CSP does not block all browser egress, including WebRTC/RTC networking. HTTP previews remain disabled pending a tested defense-in-depth solution. Use only authorized local projects."
export const HTTP_PREVIEW_BLOCKER = "HTTP preview is disabled: browser-side WebRTC/RTC networking can bypass CSP network restrictions. A tested defense-in-depth solution is required."
