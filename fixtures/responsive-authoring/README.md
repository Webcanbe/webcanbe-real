# Responsive authoring acceptance fixture

Authored for Phase 2F; not an independently sourced project. Ordinary BrowserRouter, Tailwind defaults, CSS media rules, CSS Modules, inline styles, and static const composition. The acceptance harness separately imports a labelled HashRouter variant. No editor metadata or runtime dependency is present.
