# Aperture North

An original, Webcanbe-owned studio portfolio demo built with React, TypeScript, and Vite. It includes responsive editorial sections, navigation state, varied layouts, and a generated preview image with no third-party branding.

Run `npm install` then `npm run build`.
